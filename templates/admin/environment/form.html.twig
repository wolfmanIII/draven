{% extends 'base.html.twig' %}

{% block title %}{{ environment.id ? 'Modifica environment' : 'Nuovo environment' }}{% endblock %}

{% block pageContent %}
<div class="p-6 space-y-4 max-w-4xl mx-auto">
    <div class="flex items-center justify-between gap-4">
        <h1 class="text-2xl font-bold">
            {{ environment.id ? 'Modifica environment' : 'Nuovo environment' }}
        </h1>
        <a class="btn" href="{{ path('admin_environment_index') }}">Torna alla lista</a>
    </div>

    <div class="card bg-base-100 shadow-xl border border-base-300">
        <div class="card-body space-y-4">
            {{ form_start(form) }}
                <div class="form-control">
                    {{ form_row(form.project) }}
                </div>
                <div class="form-control">
                    {{ form_row(form.name) }}
                </div>
                <div class="form-control">
                    {{ form_row(form.policy) }}
                </div>
                <div class="form-control">
                    {{ form_row(form.isEnabled) }}
                </div>
                <div class="form-control">
                    {{ form_row(form.lockStrategy) }}
                </div>
                <div class="pt-2 space-x-2">
                    <button class="btn btn-primary" type="submit">Salva</button>
                    <a class="btn" href="{{ path('admin_environment_index') }}">Annulla</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
